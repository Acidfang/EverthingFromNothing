# Security

The current explorer performs local deterministic calculation and requires no
credentials, network services or user data.

Do not include secrets in exported frames, examples, issues or pull requests.
If future versions load files or remote observations, treat all external input
as untrusted and preserve the strict separation between observed data and
executable rules.

Report security issues privately to the repository owner rather than opening a
public issue containing exploit details.
